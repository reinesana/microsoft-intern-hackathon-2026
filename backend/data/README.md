# Audio input

Drop your 911 call audio files here, then run from the `backend` folder:

```powershell
python main.py --audio data/yourcall.wav
```

Whisper accepts: .wav, .mp3, .m4a, .flac, .ogg, and more.
